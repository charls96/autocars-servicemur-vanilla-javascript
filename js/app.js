/* document.addEventListener("DOMContentLoaded", () => {
  showCars(cars);
});


function showCars(cars) {
  const body = document.querySelector('body');
  
  cars.forEach(car => {
    const carHTML = document.createElement('div');
    carHTML.innerHTML = `
      <p>${car.brand}</p>
    `;
    body.appendChild(carHTML);
  });
} */